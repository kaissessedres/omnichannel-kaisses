# URL pública da instância do Libredesk (no VM Oracle Cloud).
# Opcional: só pré-preenche o campo na tela de login (a URL/API key efetivas
# ficam no localStorage do navegador). No Vercel, defina como variável de
# ambiente do projeto. Em dev local, aponte para onde o Libredesk responde.
VITE_LIBREDESK_URL=
